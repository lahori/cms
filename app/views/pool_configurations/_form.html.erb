<%= form_for(pool_configuration) do |f| %>
  <% if pool_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pool_configuration.errors.count, "error") %> prohibited this pool_configuration from being saved:</h2>

      <ul>
      <% pool_configuration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
